[package]
name = "quote_libs"
version = "0.1.0"
edition = "2024"

[features]
logging = ["dep:log", "dep:env_logger"]

[dependencies]
log = { version = "0.4", optional = true }
env_logger = { version = "0.11", optional = true }
rand = "0.10.1"
